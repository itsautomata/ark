# ollama port (default 11434)
OLLAMA_PORT=11434

# ollama host (inside docker, "ollama" is the service name)
OLLAMA_HOST=http://ollama:${OLLAMA_PORT}

# remote: point to a GPU server running Ollama
# OLLAMA_HOST=http://your-server-ip:${OLLAMA_PORT}

# gemma model
ARK_MODEL=gemma4:e4b
